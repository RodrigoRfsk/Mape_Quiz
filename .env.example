# Variáveis do CLIENT (Vite), lidas da raiz do projeto.
# A configuração do SERVIDOR fica em server/.env (veja server/.env.example).

# Endpoint da API que recebe e persiste os leads.
# Opcional — tem default "http://localhost:3001/api/leads".
VITE_API_URL="http://localhost:3001/api/leads"
